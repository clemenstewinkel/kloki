{% extends 'base.html.twig' %}

{% block title %}KloKiEvent{% endblock %}

{% block body %}
    <h1>KloKiEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ klo_ki_event.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ klo_ki_event.name }}</td>
            </tr>
            <tr>
                <th>BeginAt</th>
                <td>{{ klo_ki_event.beginAt ? klo_ki_event.beginAt|localizeddate('medium') : '' }}</td>
            </tr>
            <tr>
                <th>EndAt</th>
                <td>{{ klo_ki_event.endAt ? klo_ki_event.endAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>AnzahlArtists</th>
                <td>{{ klo_ki_event.anzahlArtists }}</td>
            </tr>
            <tr>
                <th>IsBestBenoetigt</th>
                <td>{{ klo_ki_event.isBestBenoetigt ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>IsLichtBenoetigt</th>
                <td>{{ klo_ki_event.isLichtBenoetigt ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('klo_ki_event_index') }}">back to list</a>

    <a href="{{ path('klo_ki_event_edit', {'id': klo_ki_event.id}) }}">edit</a>

    {{ include('klo_ki_event/_delete_form.html.twig') }}
{% endblock %}
