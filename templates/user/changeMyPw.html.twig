{% extends 'base.html.twig' %}

{% block title %}Eigenes Passwort ändern{% endblock %}

{% block body %}
    <div class="container">
        <h1>Eigenes Passwort ändern</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('Speichern') }}</button>
        {{ form_end(form) }}
    </div>

{% endblock %}
